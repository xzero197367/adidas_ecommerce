@model IEnumerable<Adidas.DTOs.Separator.Category_DTOs.CategoryDto>

@{
    ViewData["Title"] = "Manage Categories";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0">Manage Categories</h2>
        <a asp-controller="Categories" asp-action="Create" class="btn btn-dark">+ Add Category</a>
    </div>

    <div class="row mb-3">
        <div class="col-md-6">
            <input type="text" class="form-control" placeholder="Search categories..." />
        </div>
        <div class="col-md-3">
            <div class="dropdown">
                <button class="btn btn-light dropdown-toggle w-100" type="button" id="dropdownMenuButtonAllCategories" data-bs-toggle="dropdown" aria-expanded="false">
                    All Categories
                </button>
                <ul class="dropdown-menu" aria-labelledby="dropdownMenuButtonAllCategories">
                    <li><a class="dropdown-item" href="#">Main Categories</a></li>
                    <li><a class="dropdown-item" href="#">Sub Categories</a></li>
                    <li><a class="dropdown-item" href="#">Uncategorized</a></li>
                </ul>
            </div>
        </div>
        <div class="col-md-3">
            <div class="dropdown">
                <button class="btn btn-light dropdown-toggle w-100" type="button" id="dropdownMenuButtonAllStatus" data-bs-toggle="dropdown" aria-expanded="false">
                    All Status
                </button>
                <ul class="dropdown-menu" aria-labelledby="dropdownMenuButtonAllStatus">
                    <li><a class="dropdown-item" href="#">Active</a></li>
                    <li><a class="dropdown-item" href="#">Inactive</a></li>
                </ul>
            </div>
        </div>
    </div>

    <div class="table-responsive">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th scope="col">IMAGE</th>
                    <th scope="col">NAME</th>
                    <th scope="col">DESCRIPTION</th>
                    <th scope="col">SLUG</th>
                    <th scope="col">SORT ORDER</th>
                    <th scope="col">STATUS</th>
                    <th scope="col">ACTIONS</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    @foreach (var category in Model)
                    {
                        <tr>
                            <td>
                                @if (!string.IsNullOrEmpty(category.ImageUrl))
                                {
                                    <img src="@category.ImageUrl" alt="@category.Name" class="img-thumbnail" style="width: 50px; height: 50px; object-fit: cover;" />
                                }
                                else
                                {
                                    <div class="product-img-placeholder">
                                        <img src="https://via.placeholder.com/50" alt="No Image" class="img-thumbnail" style="width: 50px; height: 50px; object-fit: cover;" />
                                    </div>
                                }
                            </td>
                            <td>
                                <strong>@category.Name</strong>
                                @if (category.ParentCategoryId.HasValue)
                                {
                                    <br />

                                    <small>Sub-category</small>
                                }
                            </td>
                            <td>@(string.IsNullOrEmpty(category.Description) ? "N/A" : (category.Description.Length > 50 ? category.Description.Substring(0, 50) + "..." : category.Description))</td>
                            <td>@category.Slug</td>
                            <td>@category.SortOrder</td>
                            <td>
                                <div class="form-check form-switch">
                                    <input class="form-check-input" type="checkbox" role="switch" id="statusSwitch_@category.Id" @(category.IsActive ? "checked" : "")>
                                </div>
                            </td>
                            <td>
                                <a href="#" class="btn btn-sm btn-outline-info me-1" title="View Details">
                                    <i class="bi bi-eye"></i>
                                </a>
                                <a href="#" class="btn btn-sm btn-outline-warning me-1" title="Edit">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <a href="#" class="btn btn-sm btn-outline-danger" title="Delete">
                                    <i class="bi bi-trash"></i>
                                </a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="7" class="text-center">No categories found.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <div class="d-flex justify-content-between align-items-center mt-4">
        <small>Showing 1 to @(Model != null ? Math.Min(3, Model.Count()) : 0) of @(Model != null ? Model.Count() : 0) results</small>
        <nav aria-label="Page navigation example">
            <ul class="pagination mb-0">
                <li class="page-item disabled">
                    <a class="page-link" href="#" aria-label="Previous">
                        <span aria-hidden="true">Previous</span>
                    </a>
                </li>
                <li class="page-item active"><a class="page-link" href="#">1</a></li>
                <li class="page-item"><a class="page-link" href="#">2</a></li>
                <li class="page-item"><a class="page-link" href="#">3</a></li>
                <li class="page-item">
                    <a class="page-link" href="#" aria-label="Next">
                        <span aria-hidden="true">Next</span>
                    </a>
                </li>
            </ul>
        </nav>
    </div>
</div>

@section Scripts {
    <script>
        // You might want to add JavaScript here for:
        // 1. Search functionality (filtering the table based on search input)
        // 2. Dropdown filtering (by category type or status)
        // 3. Handling the status switch (e.g., making an AJAX call to update category status)
        // 4. Pagination logic (if you implement server-side pagination)
    </script>
}